{
  "devDependencies": {
    "@babel/core": "^7.16.0",
    "@babel/preset-env": "^7.16.0",
    "babel-jest": "^27.3.1",
    "eslint": "^8.2.0",
    "html-validate": "^6.1.2",
    "jest": "^27.3.1",
    "jest-puppeteer": "^6.0.0",
    "puppeteer": "^11.0.0"
  },
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "preset": "jest-puppeteer",
    "verbose": true
  },
  "prettier": {
    "tabWidth": 4,
    "overrides": [
      {
        "files": "*.js",
        "options": {
          "singleQuote": true
        }
      },
      {
        "files": "*.json, *yml",
        "options": {
          "tabWidth": 2
        }
      }
    ]
  }
}
